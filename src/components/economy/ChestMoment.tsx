// Chest opening moment (BUILD PROMPT Section 5). Full ceremony + logic land in
// Step 8; this stub renders nothing until a pending chest is ready to open.
export function ChestMoment() {
  return null;
}
